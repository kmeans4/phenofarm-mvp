# Ongoing backups — September 24, 2026

Implemented the launch-review request to protect production records and uploaded files. See [operations and recovery](../operations/backups.md).

## Evidence

- Live Neon settings changed from 21,600 seconds / no schedule to 604,800 seconds and daily 07:00 UTC snapshots retained 3,024,000 seconds. Schedule/settings were read back independently.
- Fresh snapshot `snap-sparkling-brook-aiehmvdq` completed at 33,406,976 bytes. The original production endpoint remains on `br-wandering-snow-aig9frhe`.
- Added a dedicated read-only database login; checked all 22 tables readable and zero tables writable. No business records were modified by setup.
- Local database dump and archived-copy recovery both passed in isolated PostgreSQL 17 containers. Media backup contained all seven source files (54,757 bytes); file checksums and database URL-reference coverage passed.
- Private repository `kmeans4/phenoshop-backups` was created for the operations workflow. Its environment is restricted to `main`, and the workflow refuses to run with credentials if repository visibility or branch differs.
- First cloud run [36037638804](https://github.com/kmeans4/phenoshop-backups/actions/runs/36037638804) passed: live database/media read, restore, archive upload, exact retained-artifact download, hashes, and isolated restore. Artifact `10824434892` has explicit expiry October 29, 2026 (35 days).
- Five focused regression checks cover stale/future timestamps, wrong database/role targets, full upload pagination, missing pagination cursors, and secret redaction.
- The support alert test was accepted by Resend and subsequently reported `delivered` for `support@phenoshop.app`.
- No app deployment is needed. Application storage/auth/DB connection settings and unrelated untracked review work are preserved.

## Final verification

- Final backup code commit `12e70e2` is pushed to the private operations repository.
- Final cloud run [36038164370](https://github.com/kmeans4/phenoshop-backups/actions/runs/36038164370) passed the complete backup, artifact retention, downloaded-copy checksum, and isolated recovery pipeline. The immediately preceding streamlined run `36037949280` also passed.
- Repository visibility is PRIVATE; an unauthenticated GitHub API request returned 404. The workflow is active with both cron entries. Native daily snapshots are configured independently of GitHub.
- Final provider readback again confirmed the 604,800-second recovery window and daily 07:00 UTC / 3,024,000-second snapshot policy.
- The live application returned HTTP 200 with its PhenoShop title after setup. No web deployment was performed or needed.
- Future scheduled runs have not happened yet; current proof consists of saved schedules plus successful manual runs of the exact scheduled workflow. Inspect the next scheduled run in Actions as part of normal operations.
- No further Vercel login is required. The unused empty extra store was removed; native Neon plus private GitHub archives provide the active coverage.

