# Independent second-pass observations — root

##14Orders list
Fresh desktop1440/mobile390 screenshots saved under pass-2 and inspected. Desktop dense data table remains readable but60px comfort rows +large KPI/form preamble consume height; default compact density is worth considering for desktop. “Saved workflow views” is a verbose/misleading name for status tabs; use a Status label or just tabs and remove instruction to start with next request state. All(18) duplicates Active Requests(18) and topActiveRequests18. Settlement subtitle repeats routine policy. Mobile table leaves status/value/View offscreen; prioritize compact mobile request rows with buyer/date/status/value and a visible action rather than hiding those to the right. Preserve a short ID with full value available when needed. No app actions submitted.

##15Request History
Freshdesktop/mobile inspected. Four separate109–141px mobile metrics create nearly500px of preamble for just2records; use2×2counts and compactvalue. “View delivered and cancelled requests only” and “Delivered & Cancelled Requests” duplicate History context; use a short title and status tabs. Back to Active Requests -> Active requests. Mobile table has long IDs wrapping3–4lines; estimatedvalue/status/action are partly or fully offscreen. Cards with shortID/customer/date/value/status are preferable. Desktop empty space below two records is data-driven, not a problem.

##16Record Direct Request
Freshdesktop/mobile inspected. Shorten heading/button to Record request; intro to “Log a request agreed directly with a buyer.” “outside PhenoFarm payment rails” is technical/redundant. Buyer search helper repeats placeholder; one concise label/placeholder enough. Put Items before optional Notes/shipping; mobile Items beginsy769, below two full-width action buttons and verbose empty-input panel. Shipping numeric field spans entire1100px desktop form for a smallvalue; constrain/combine shortfields. Empty-items dashedbox135px +two instruction sentences can become one compact Add item row. NEW: mobile floating messages button partly overlaps Add item button at right edge (x334–378/y788–832 vs Add x289–357/y769–809); reserve launcher clearance or reposition at this section. No item-row interactions performed here (agent owns16a).

##17Request detail
Freshdesktop/mobile inspected. Long32/36px ID title wraps; use concise Request heading and smaller copyable fullID metadata. Mobile has Back to requests, All Requests and sticky All orders plus two Edit Request actions; consolidate navigation/actions and vocabulary. Print/Export secondary actions can share More menu. Primary Accept Request is below items/details while stickybar promotes Edit; show next fulfillment action in primary position. Submitted repeats topbadge/updatecard/timeline/currentstatus/history; collapse duplicated current-state copy and future pending stages. Desktop right-only tower (status,timeline,history,customer) leaves large lower-left void; rebalance customer/history below items or compact timeline. NEW mobile customer address concatenates “456Market StreetBurlington, VT05401” without separator; keep line break or comma in compact form. Existing item line has price twice(subtotal/total) for single item; totals need not remove important distinction globally, but reduce panelpadding/disclosure copy.

##18Edit request
Freshdesktop/mobile inspected. Existing compact title+smallerID works better than detail heading. Submitted appears both beside header and in84px current-status block; combine into one status header. Mobile status panel310px tall for two possible transitions; use concise “Next status” and compact options while retaining targets. Shipping/tax short numeric fields can share a row; trim helpers to “Shipping estimate” and “Enter tax from invoice.” Keep recorded-tax meaning explicit. Request Notes repeats Request context ->Notes, Save Changes ->Save request. Request summary padding can tighten but preserve transparent tax/shipping/total figures. No overflow; quantity control fits and max241 is intentionally original reserved unit +240available, not a bug.

##19Customers
Freshdesktop/mobile inspected. Mobile three stacked metrics consume~295px before list startsy517; combine into compactrow/grid. Subtitle too long -> “Saved contacts and buyer relationships.” Customer List repeats Customersheading, remove. Mobile records list Contact/Email/Phone/Location/Lastorder/Deliveredvalue as separate labeledlines; combine metadata into fewer lines while retaining essentials. For platform buyer Contact duplicates businessname exactly—omit duplicate. Desktopemail+phone are alreadycompact12px; do not shrink further. NEW discoverability issue: no Add customer button, and savedcontact row only Viewrequests/Message, despite functioning Add/Edit/Statement routes; provide entrypoints if these supported management screens are intended for everyday use. No mutation performed.

##20Add customer
Freshdesktop/mobile inspected. “Add New Customer” +“Add a new dispensary customer” +“Customer Information” triple repeats task; use Add customer and go directly to fields. City/State/ZIP become three full-width mobile rows; keep City then State+ZIP side-by-side to saveonefieldrow without shrinking text. Reduce oversized between-field gaps modestly; keep16px inputtext and tappableheights. Short labels already clear. Cancel/Add customer can sitinline onmobile aftershorttitle. Desktop grid readswell; blank area below form is not a problem.

##21Edit customer
Freshdesktop/mobile inspected. View statement floats on its own top row, then Back to Customers, then heading atmobiley214; consolidate them into one compact header so form starts earlier. “Update customer information” repeats Edit Customer; omit. Business Information/Contact Information ->Business/Contact. Match Add form labels (Contact name, ZIP) rather than Contact Person/ZIP Code. State+ZIP can share mobile row; website/description are optional and could sit under an optionaldetails reveal when empty. Desktop form narrower(768px) than Add customer(1136px), with separate floating statement action outsideform; adopt consistent content width/header placement. Keep destructive Delete separate from Save.

##22Customer statement
Freshdesktop/mobile inspected. Mobile two-line32px title pluslongintro/exportseparaterow precede149px daterangebox; use Statement heading with smaller customername and compact export/action placement. Keep From/To two-column fields (alreadybetter than otherforms), tighten wrapperpadding and Apply placement ifpossible. Header “1 delivered requests” needs singular. Settlement intro +footer repeats sameboundary; one succinctnotice enough. Mobile onlyRequest/Delivered columns visible; Items/Value are offscreen, so use compactrows preserving amount (overall totalalone doesn'treplace per-requestvalue). Desktop empty area below one record is sampledata, notfiller opportunity. Date displayedSep15 for synthetic deliveredAtSep16midnight likelysameUTC date-onlyshift asbatch/reports—validate actualfixture field beforecallingbug.
Statement date adjudication: deliveredAt is an instant, and synthetic fixture was created at2026-09-16T00:00Z. Sep15evening local display is expected conversion, so DO NOT report statement delivered date as a bug from this fixture. This differs from calendar-month labels and harvest-date form inconsistencies.
