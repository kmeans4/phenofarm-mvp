# PhenoShop branding release — September 24, 2026

The owner selected logo option 03, Leaf Cutout: an emerald P with a leaf-shaped counter, integrated into the white PhenoShop wordmark.

## Changes

- Shared original-artwork logo across landing navigation/footer, help, legal pages, signup/sign-in/error/recovery screens, not-found page, and grower/buyer/admin portal headers. The landing product preview and money-flow diagram use the matching P mark.
- Replaced app-controlled PhenoFarm branding in visible copy, search, metadata, notification/message templates, support-mail subjects, product-import example branding, and CSV/PDF export text and filenames.
- Browser favicon contains only the P: 16, 32, and 48px ICO entries plus 32px PNG. Mark height is approximately 87.5% of the square, centered with proportional width. Apple touch icon is 180px, with 192/512px manifest icons. OpenGraph/Twitter images use the selected wordmark.
- Vercel production `AUTH_MAIL_FROM` updated to `PhenoShop <accounts@phenoshop.app>` for this release. Sender address, provider, reply-to address, verification enforcement, and delivery logic are unchanged.
- Preserved browser storage keys, event names, local database guards, domain redirects, and provider/project identifiers to avoid losing drafts or breaking integrations. Historical review documents and business-entered content are not rewritten.

## Verification

- Browser checks: public/account/legal routes at 320px; grower, buyer, and admin headers at 1440px and 320px using actual credential login to three isolated local fixtures.
- Logo/mark source comparison, light/dark favicon checks at actual sizes, header fit, navigation, and renamed search dialog passed. See project-root `design-qa.md`.
- Local target was `localhost/phenofarm_auth_editor_20260924`. The older default local database lacked an account-security column; it was not migrated or modified. All three fixtures were removed from the isolated database after review.
- Account-mail check exercised the real reset-link function with an intercepted transport: subject, signature, and configured sender used PhenoShop. Zero external messages were sent by this test.
- Source scan found no remaining PhenoFarm or PF brand labels in app-controlled source text.

## Release

- `npm run verify` passed: environment documentation, ESLint, TypeScript, and the production build (103 routes).
- No database migration is required. Production release contains only the committed branding changes and evidence; unrelated untracked review files were excluded.

- Code commit `a11e811d527983d058e63437ee8e1c8edae8fbd4` pushed to `origin/main`; GitHub Verify run `36031674638` passed.
- Deployed a clean archive of that commit to Vercel production. Deployment `dpl_FpeCWEdGDFY28uu8GMzgmNQSbDiM` is READY and resolves through `https://phenoshop.app` (`phenofarm-l3pczdd7u-kevin-means-projects.vercel.app`).
- Live homepage visually reviewed at 1440×1000 and 320×800; no horizontal overflow or old branding. All eleven public/account/legal pages passed the mobile brand, loaded-logo, and width checks after loading completed. Browser error log was empty.
- Eight live logo/icon/share-image URLs returned HTTP 200 with expected MIME types and byte-for-byte parity with the committed assets. Manifest name/short name are PhenoShop; browser head advertises the ICO, 32px PNG, and 180px Apple icon.
- Live evidence: `/tmp/phenoshop-brand-20260924/live-1440.png`, `live-320.png`, `live-public-checks.json`, `live-assets.json`, and `deployment.json`.
- This follow-up release record changes documentation only; the live application matches the tested code commit above.
